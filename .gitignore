# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# XcodeGen generates the project from project.yml — don't commit it
GraftBar.xcodeproj/

# macOS
.DS_Store

# Graft runtime state & secrets (never commit)
.graft/
*.pem
config.local.json
