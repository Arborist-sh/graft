{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Graft image recipe",
  "type": "object",
  "required": ["name", "from"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "The local image name produced by the build."
    },
    "from": {
      "type": "string",
      "description": "Base Tart image to clone, e.g. ghcr.io/cirruslabs/macos-sequoia-xcode:latest"
    },
    "node": {
      "type": ["string", "number"],
      "description": "Node version — expands to fnm install/default + corepack + a stable /usr/local/bin node symlink. Quote bare majors (\"20\")."
    },
    "ruby": {
      "type": ["string", "number"],
      "description": "Ruby version — expands to rbenv install + global + bundler."
    },
    "brew": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Homebrew formulae to install."
    },
    "gems": {
      "type": "array",
      "items": { "type": "string" },
      "description": "RubyGems to install (gem install … --no-document)."
    },
    "npm": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Global npm packages (npm install -g …)."
    },
    "xcode-first-launch": {
      "type": "boolean",
      "description": "Run `sudo xcodebuild -runFirstLaunch` to install Xcode components headlessly."
    },
    "warm-simulators": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Simulator devices to cold-boot once (warms on-disk caches), then shut down. E.g. [\"iPhone 17 Pro\"]."
    },
    "run": {
      "description": "Escape hatch: raw bash. A `|` block (one script) or a list of steps. Runs after the declarative fields.",
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "script": {
      "type": "string",
      "description": "Path (relative to this recipe) to a shell script run in the guest. Runs before `run`."
    },
    "os": {
      "type": "string",
      "enum": ["macos", "linux"],
      "description": "Guest OS (default macos)."
    },
    "mounts": {
      "type": "array",
      "description": "Host directories shared into the build VM.",
      "items": {
        "type": "object",
        "required": ["name", "source"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "description": "Mount tag → /Volumes/My Shared Files/<name> in the guest." },
          "source": { "type": "string", "description": "Host path." },
          "readOnly": { "type": "boolean", "description": "Mount read-only (default false)." }
        }
      }
    }
  }
}
